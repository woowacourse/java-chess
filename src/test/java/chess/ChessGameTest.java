package chess;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;

class ChessGameTest {

    @Test
    void name() {
//        final ChessBoard chessBoard = ChessBoard.init();
//        final List<List<PieceWithColor>> chessBoardDetail = chessBoard.getBoard();
//        final PieceWithColor current = chessBoardDetail.get(1).get(1);
//
//        chessBord.move(b2, b3);
//        chessBoard.findBy(b2);
//        chessBoard.findBy(b3);
//
//        //current.moveTo(b3);
//        final PieceWithColor next = chessBoardDetail.get(2).get(1);
//
//        assertThat(next).isEqualTo(current);
//        assertThat(current).isEqualTo(new PieceWithColor(Piece.EMPTY, PieceColor.EMPTY));
    }
}
