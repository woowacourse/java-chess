package chess.domain.piece;

import static org.assertj.core.api.Assertions.assertThat;

class PawnTest {

//    @ParameterizedTest
//    @DisplayName("#pawnCalculateScore() : should return score of pawn as to board and position")
//    @MethodSource({"getCasesForCalculateScore"})
//    void pawnCalculateScore(Position position, Score expected) {
//        //given
//        Piece pawn = null;
//        PiecesState boardState = TestPiecesState.initialize();
//        //when
//        Score score = pawn.pawnCalculateScore(boardState);
//        //then
//        assertThat(score).isEqualTo(expected);
//    }
//
//    private static Stream<Arguments> getCasesForCalculateScore() {
//        return Stream.of(
//                Arguments.of(Position.of(1,2), new Score(1)),
//                Arguments.of(Position.of(2,3), new Score(0.5))
//        );
//    }
}