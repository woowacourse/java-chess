package chess.domain.piece;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class ShapeTest {

    @DisplayName("룩이 이동할 수 있는 위치인지 학인하는 기능")
    @Test
    void isRightPath_Rook() {
//        boolean isRightPath = Shape.ROOK.isRightPath(new Position(0, 0), new Position(7, 0), board);
//        boolean isFailPath = Shape.ROOK.isRightPath(new Position(0, 0), new Position(7, 1), board);
//
//        assertThat(isRightPath).isTrue();
//        assertThat(isFailPath).isFalse();
    }

    @DisplayName("나이트가 이동할 수 있는 위치인지 학인하는 기능")
    @Test
    void isRightPath_Night() {
//        boolean isRightPath = Shape.NIGHT.isRightPath(new Position(0, 0), new Position(3, 1), board);
//        boolean isFailPath = Shape.NIGHT.isRightPath(new Position(0, 0), new Position(3, 0), board);

//        assertThat(isRightPath).isTrue();
//        assertThat(isFailPath).isFalse();
    }

}